#include <string>

using std::string;

vector<string> allSubstrings (const std::string& s) {
  if (s.size() == 0) s.push_back();
  std::vector<int> f;
  f.push_back(3);
  std::string s1;
  s1.append
}
